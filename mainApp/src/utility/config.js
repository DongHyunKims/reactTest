
const config = {
    DEFAULT_URL : "http://localhost:3000",
    MAX_SIZE : 2 * 1024 * 1000,
    TAG_MAX_LEN : 5,
    TAG_STR_MAX_LEN : 10,
    INIT_DATA : {_id:"1",name:"없음", img:"", tag:["없음"]},
    REG_ALPHA : /^[A-Za-z]*$/,
};

export default config;